{
  "name": "well-seasoned-app",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor native wrapper for the Well Seasoned PWA (itswellseasoned.com). This folder is isolated from the main site, which stays a zero-build single-file app — this only packages the live site for the App Store / Google Play.",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
